/**
 Function class that implements the sine function
*/
public class SinFunc extends Function {
	/**
	 Evaluates sin(x) where x = x.
	 @param x Value to plug in for sin(x).
	 @return sin(x) where x is plugged in for x.
	*/
	public double evaluate(double x) {
		return Math.sin(x);
	}
}
